authelia:
  configMap:
    theme: oled
    session:
      remember_me: "7 days"
      cookies:
        - domain: "127.0.0.1"
          authelia_url: "http://127.0.0.1:9091"
          insecure: true
    identity_providers:
      oidc:
        clients:
          - client_id: "insidan"
            client_name: "Insidan"
            consent_mode: "implicit"
            pre_configured_consent_duration: "1y"
            client_secret:
              path: "/secrets/authelia-secrets/client-insidan-secret"
            public: false
            authorization_policy: "one_factor"
            redirect_uris:
              - "http://localhost:3000/api/auth/callback"
              - "http://localhost:8000/api/auth/callback"
              - "https://insidan.linkopingsfallskarmsklubb.se/api/auth/callback"
            scopes: ["openid", "profile", "email", "groups"]
            grant_types: ["authorization_code"]
          - client_id: "adsb-parser"
            client_name: "ADSB Parser"
            client_secret:
              path: "/secrets/authelia-secrets/client-adsb-parser-secret"
            public: false
            authorization_policy: "bypass"
            claims_policy: "adsb_parser_claims"
            scopes: ["openid", "profile", "email", "groups"]
            grant_types: ["client_credentials"]
    access_control:
      rules:
        - domain: "127.0.0.1"
          policy: "one_factor"

  ingress:
    enabled: false
