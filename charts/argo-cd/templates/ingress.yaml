{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  {{- if .Values.host }}
  annotations:
    {{- if ne .Values.tls false }}
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    {{- if .Values.certResolver }}
    traefik.ingress.kubernetes.io/router.tls.certresolver: {{ .Values.certResolver }}
    {{- end }}
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: web
    {{- end }}
  {{- end }}
spec:
  rules:
    {{- if .Values.host }}
    - host: {{ .Values.host }}
    {{- else }}
    - host:
    {{- end }}
      http:
        paths:
          - path: {{ .Values.path | default "/" | quote }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-argocd-server
                port:
                  number: 80
  {{- if and .Values.host (ne .Values.tls false) }}
  tls:
    - hosts:
      - {{ .Values.host }}
  {{- end }}
{{- end }}
